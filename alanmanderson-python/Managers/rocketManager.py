import battlecode as bc

class RocketManager():
  def __init__(self, controller):
    self.controller = controller

  def handle(self, unit):
    pass
#    print(self.controller.starting_map(self.controller.planet().other()).to_json())
#    if len(unit.structure_garrison()) == unit.structure_max_capacity():
#      if self.controller.can_launch(unit.id, ):

